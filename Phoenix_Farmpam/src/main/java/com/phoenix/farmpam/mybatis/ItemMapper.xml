<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="item">
	<!-- 상품 목록 -->
	<select id="itemList" resultType="com.phoenix.farmpam.item.dto.ItemDto">
		SELECT
		item_idx, item_image, item_title, item_content, item_price, item_stock
		FROM item
		OREDE BY item_idx
	</select>
</mapper>