<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="farmer">
	<select id="isExist" parameterType="java.lang.String" resultType="string">
		SELECT farmer_email 
		FROM farmer
		WHERE farmer_email=#{farmer_email}
	</select>
	<insert id="insert" parameterType="com.phoenix.farmpam.farmer.dto.FarmerDto">
		INSERT INTO farmer
		(farmer_email, farmer_pwd, farmer_name, farmer_addr, farmer_addr_detail, farmer_phone)
		VALUES(#{farmer_email}, #{farmer_pwd }, #{farmer_name }, #{farmer_addr }, #{farmer_addr_detail }, #{farmer_phone}) 
	</insert>
</mapper>